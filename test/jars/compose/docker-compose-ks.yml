version: '3'
services:
  ks-trace:
    image: imcloud/ks-trace
    networks:
      - springcloud-overlay
    environment:
      - EUREKA_SERVER_ADDRESS=core-eureka

networks:
  springcloud-overlay:
    external:
      name: springcloud-overlay

