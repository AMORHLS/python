version: '3'
services:
  base-sms:
    image: imcloud/base-sms
    networks:
      - springcloud-overlay
    environment:
      - EUREKA_SERVER_ADDRESS=core-eureka

networks:
  springcloud-overlay:
    external:
      name: springcloud-overlay

